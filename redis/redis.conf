# Redis configuration for the URL shortener.
# Redis is used purely as a cache plus rate-limit / JWT-denylist store — the
# source of truth is Postgres, so a bounded LRU cache is the desired behavior.
# Tune these per deployment (e.g. raise maxmemory in production).

maxmemory 64mb
maxmemory-policy allkeys-lru
